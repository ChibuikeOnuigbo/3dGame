# Session 2026-09-07 — black-wall extermination + surroundings pass

## Attachment diagnostic (as instructed)
Ran the filesystem diagnostic first: `/home/user/uploads`, `/home/oai/share`,
`/mnt/data`, `/workspace`, `/workspaces` do NOT exist; `/tmp` was empty.
**ATTACHMENT_NOT_MOUNTED** — no user attachment reached this runtime. No fake
placeholders were created; all visual evidence below is rendered from the
game itself (headless Chromium) and analysed with OpenCV.

## Network matrix this session
| Host | Reachable | Consequence |
| --- | --- | --- |
| github.com (git smart-http) | **auth-broken** (session token expired: `Bad credentials`; clone of even public repos prompts for username) | re-clones blocked; push/PR need a GitHub reconnect in Arena |
| api.github.com | 401 (same dead token) | search API unusable |
| codeload / raw.githubusercontent | 404 / blocked | tarball fallback unusable |
| registry.npmjs.org | OK | dev deps + QA chromium install only |
| sketchfab.com / api.sketchfab.com | blocked | model downloads impossible regardless of token |
| polyhaven.com | blocked | textures impossible (ambientCG CC0 already shipped via fps-asset-kit study) |
| opengameart.org | blocked | SFX downloads impossible → jump/land/run SFX synthesized procedurally; CC0 Fantozzi FLAC footsteps retained |
| itch.io | blocked | pack pages evidence-only |
| youtube.com | blocked | yt-dlp extraction impossible (prior video study in research/video_analysis remains the reference) |

The 2026-09-04 license matrix (research/licenses/GITHUB_LICENSE_MATRIX.md)
still governs: only clearly-CC0/attributed material ships. The nine GitHub
repos were re-read via that study; re-cloning this session was blocked by the
expired token (see above — user asked to be told when GitHub needs attention).

## What the ghost scanner found (and fixed)
`tools/qa/scan_ghosts.py` enumerates every mesh (opacity/transparency/luma/
double-side/canvas-text), matches against colliders and door volumes, then
walks a dummy capsule over a 0.45 m grid of the whole map (all doors opened):

1. fence infill planes mis-rotated 90° → three huge 0.42-opacity dark planes
   across the parcel, one through door_street, extending to z=-30 (outside
   the map) — the reported black wall. Re-aligned, then rebuilt as an
   alpha-tested chain-link lattice.
2. tomb-gate bars at world origin inside door_d1 threshold; whole gate buried
   in the sump west wall → rebuilt proud of the wall.
3. skyline blocks (30,11,-8) and (26,16,6) intruded into the playable bounds
   as dark walk-through masses → moved to x>=31.5.
4. bushes / beater pump / gantry rails got colliders; sump pipe re-hung above
   head height; kiosk facade slits closed; exterior ground disc added; the
   `always` light circuit was never enabled (street lamp dead since v1.0) →
   enabled; `_tryAxis` far-face teleport on pre-overlapped colliders fixed.

Post-fix: 0 open cells outside rooms, 0 invisible walls, no walk-through
wall-scale ghosts (remaining probe hits are ramp/step underfoot grazing and
the camera-following torch viewmodel), OpenCV black-wall flags: NONE.

## Gates
verify_world 16/16 · critical_path 40/40 · smoke console-clean ·
ghost scan clean · OpenCV flags NONE.
